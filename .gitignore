# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
*.egg-info/
build/
dist/
.venv/
venv/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# Skill build artifacts — built by CI on tag push, attached to GitHub releases.
# Do NOT commit *.skill files; they are release artifacts, not source.
*.skill
description-optimization/
trigger-eval.json
eval_review.html

# Eval workspace (per-iteration test outputs are not committed; regenerate locally)
user-story-mapping-workspace/

# Project-local memory state (per-user, opt-in)
.user-story-mapping/
